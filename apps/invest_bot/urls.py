from django.urls import path
from . import views

app_name = 'invest_bot'

urlpatterns = [
    # path('webhook/', views.TelegramWebhookView.as_view(), name='webhook'),  # Bu qatorni kommentga oling yoki o'chirib tashlang
    # Boshqa endpointlar
] 